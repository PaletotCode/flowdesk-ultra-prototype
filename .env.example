# Configurações do Banco de Dados
# URL de conexão com PostgreSQL (Railway ou outro provedor)
DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco

# Configurações do Google Cloud Storage
# Caminho para o arquivo JSON de credenciais do GCS
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Alternativamente, você pode definir as credenciais como variáveis de ambiente:
# GOOGLE_CLOUD_PROJECT=seu-projeto-gcp
# GOOGLE_CLOUD_STORAGE_BUCKET=seu-bucket-padrao

# Configurações do Servidor
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Configurações de Logging
SQL_ECHO=false  # Define como 'true' para ver as queries SQL nos logs

# Configurações de Segurança (para futuras implementações)
# SECRET_KEY=sua-chave-secreta-super-segura
# JWT_ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Configurações de Environment
ENVIRONMENT=production  # ou 'development', 'staging'